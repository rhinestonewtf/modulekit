RegistryHookTest::onInstall
├── when module is intialized
│   └── it should revert
└── when module is not intialized
    ├── it should set the registry of msg.sender
    └── it should emit RegistrySet


RegistryHookTest::onUninstall
└── it should remove the registry


RegistryHookTest::isInitialized
├── when module is not intialized
│   └── it should return false
└── when module is intialized
    └── it should return true


RegistryHookTest::setRegistry
├── it should set the registry of msg.sender
└── it should emit RegistrySet


RegistryHookTest::preCheck
├── when function is not installModule
│   └── it should return
└── when function is installModule
    ├── when module is not attested
    │   └── it should revert
    └── when module is attested
        └── it should return


RegistryHookTest::postCheck
└── it should return


RegistryHookTest::name
└── it should return RegistryHook


RegistryHookTest::version
└── it should return 1.0.0


RegistryHookTest::isModuleType
├── when typeID is 4
│   └── it should return true
└── when typeID is not 4
    └── it should return false