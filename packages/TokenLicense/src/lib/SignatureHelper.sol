// SPDX-License-Identifier: MIT
pragma solidity ^0.8.17;

library SignatureHelper {
    function witnessDigest(
        uint256 chainId,
        address module,
        uint256 totalAmount,
        address token
    )
        internal
        returns (bytes32)
    { }
}
